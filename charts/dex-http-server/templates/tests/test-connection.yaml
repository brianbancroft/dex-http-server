apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dex-http-server.fullname" . }}-test-connection"
  labels:
    {{- include "dex-http-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dex-http-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
